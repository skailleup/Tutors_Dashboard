import React from "react";
import Logo from "../logo";

const Sidebar: React.FC = () => {
  return (
    <header className="bg-primary-default p-2 text-white">
      <Logo />
    </header>
  );
};

export default Sidebar;
